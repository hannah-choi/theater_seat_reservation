import React from "react";

export default function Calculate() {
    return (
        <div className="calculate">
            You have selected <span className="seatTotal">00</span>seats for a
            price of £<span className="priceTotal">00</span>
        </div>
    );
}
